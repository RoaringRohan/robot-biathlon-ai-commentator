# Copy to web/.env.local (and training/.env for the pipeline). Never commit the real file.

# Gemini - the AI referee that reads camera frames and calls the run
GEMINI_API_KEY=""

# ElevenLabs - turns the referee's commentary cue into speech
ELEVENLABS_API_KEY=""

# Snowflake - stores run footage and backs the retraining pipeline
SNOWFLAKE_ACCOUNT=""
SNOWFLAKE_USER=""
SNOWFLAKE_PASSWORD=""
SNOWFLAKE_DATABASE=""
SNOWFLAKE_SCHEMA=""
SNOWFLAKE_WAREHOUSE="COMPUTE_WH"

# Deploy target for training/deploy.ps1 and training/get_demo.ps1
DROPLET_IP=""
